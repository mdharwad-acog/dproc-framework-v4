<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PubMed Research Analysis: {{ inputs.query }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Georgia', 'Times New Roman', serif;
            line-height: 1.8;
            color: #1a202c;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 40px 20px;
        }
        .container {
            max-width: 1000px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 60px 40px;
            text-align: center;
        }
        .header h1 {
            font-size: 2.5em;
            margin-bottom: 20px;
            font-weight: 700;
        }
        .header .meta {
            font-size: 1.1em;
            opacity: 0.95;
        }
        .content {
            padding: 50px;
        }
        .section {
            margin-bottom: 50px;
        }
        .section h2 {
            color: #667eea;
            font-size: 2em;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 3px solid #667eea;
        }
        .section h3 {
            color: #764ba2;
            font-size: 1.5em;
            margin: 30px 0 15px 0;
        }
        .summary-box {
            background: linear-gradient(135deg, #f6f8ff 0%, #f0e6ff 100%);
            padding: 30px;
            border-radius: 15px;
            border-left: 5px solid #667eea;
            margin: 20px 0;
            font-size: 1.1em;
        }
        .findings-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin: 20px 0;
        }
        .finding-card {
            background: #f7fafc;
            padding: 20px;
            border-radius: 10px;
            border-left: 4px solid #48bb78;
        }
        .theme-card {
            background: white;
            border: 2px solid #e2e8f0;
            padding: 25px;
            border-radius: 12px;
            margin: 15px 0;
            transition: all 0.3s;
        }
        .theme-card:hover {
            border-color: #667eea;
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.2);
        }
        .theme-card h4 {
            color: #667eea;
            font-size: 1.3em;
            margin-bottom: 10px;
        }
        .badge {
            display: inline-block;
            background: #667eea;
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9em;
            font-weight: 600;
        }
        .paper-card {
            background: #fff5f5;
            border-left: 4px solid #f56565;
            padding: 20px;
            margin: 15px 0;
            border-radius: 8px;
        }
        .paper-card h4 {
            color: #c53030;
            margin-bottom: 10px;
        }
        .list-styled {
            list-style: none;
            padding: 0;
        }
        .list-styled li {
            padding: 12px 0;
            padding-left: 30px;
            position: relative;
            border-bottom: 1px solid #e2e8f0;
        }
        .list-styled li:before {
            content: "→";
            position: absolute;
            left: 0;
            color: #667eea;
            font-weight: bold;
            font-size: 1.2em;
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin: 30px 0;
        }
        .stat-box {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            border-radius: 15px;
            text-align: center;
        }
        .stat-box .number {
            font-size: 3em;
            font-weight: bold;
            display: block;
            margin-bottom: 10px;
        }
        .stat-box .label {
            font-size: 1em;
            opacity: 0.9;
        }
        .footer {
            background: #f7fafc;
            padding: 30px 50px;
            border-top: 3px solid #e2e8f0;
            color: #718096;
            font-size: 0.9em;
        }
        .footer-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>📚 PubMed Research Analysis</h1>
            <div class="meta">
                <p style="font-size: 1.3em; margin: 10px 0;">"{{ inputs.query }}"</p>
                <p>{{ data.searchDate | date('MMMM D, YYYY') }}</p>
            </div>
        </div>

        <!-- Content -->
        <div class="content">
            <!-- Statistics -->
            <div class="stats-grid">
                <div class="stat-box">
                    <span class="number">{{ data.totalFound }}</span>
                    <span class="label">Total Papers Found</span>
                </div>
                <div class="stat-box">
                    <span class="number">{{ data.fetchedCount }}</span>
                    <span class="label">Papers Analyzed</span>
                </div>
                <div class="stat-box">
                    <span class="number">{{ llm.keyFindings | length }}</span>
                    <span class="label">Key Findings</span>
                </div>
            </div>

            <!-- Executive Summary -->
            <div class="section">
                <h2>📋 Executive Summary</h2>
                <div class="summary-box">
                    {{ llm.executiveSummary }}
                </div>
            </div>

            <!-- Key Findings -->
            <div class="section">
                <h2>💡 Key Findings</h2>
                <div class="findings-grid">
                    {% for finding in llm.keyFindings %}
                    <div class="finding-card">
                        <strong>{{ loop.index }}.</strong> {{ finding }}
                    </div>
                    {% endfor %}
                </div>
            </div>

            <!-- Major Themes -->
            <div class="section">
                <h2>🎯 Major Research Themes</h2>
                {% for theme in llm.majorThemes %}
                <div class="theme-card">
                    <h4>{{ theme.theme }}</h4>
                    <p>{{ theme.description }}</p>
                    <p style="margin-top: 10px;">
                        <span class="badge">{{ theme.paperCount }} papers</span>
                    </p>
                </div>
                {% endfor %}
            </div>

            <!-- Methodologies -->
            <div class="section">
                <h2>🔬 Common Methodologies</h2>
                <ul class="list-styled">
                    {% for method in llm.methodologies %}
                    <li>{{ method }}</li>
                    {% endfor %}
                </ul>
            </div>

            <!-- Research Gaps -->
            <div class="section">
                <h2>🔍 Identified Research Gaps</h2>
                <ul class="list-styled">
                    {% for gap in llm.researchGaps %}
                    <li>{{ gap }}</li>
                    {% endfor %}
                </ul>
            </div>

            <!-- Future Directions -->
            <div class="section">
                <h2>🚀 Future Research Directions</h2>
                <ul class="list-styled">
                    {% for direction in llm.futureDirections %}
                    <li>{{ direction }}</li>
                    {% endfor %}
                </ul>
            </div>

            <!-- Top Papers -->
            <div class="section">
                <h2>⭐ Most Significant Papers</h2>
                {% for paper in llm.topPapers %}
                <div class="paper-card">
                    <h4>{{ paper.title }}</h4>
                    <p><strong>PMID:</strong> <a href="https://pubmed.ncbi.nlm.nih.gov/{{ paper.pmid }}/" target="_blank">{{ paper.pmid }}</a></p>
                    <p style="margin-top: 10px;"><strong>Significance:</strong> {{ paper.why }}</p>
                </div>
                {% endfor %}
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <div class="footer-grid">
                <div>
                    <p><strong>Generated:</strong> {{ metadata.timestamp }}</p>
                    <p><strong>Processing Time:</strong> {{ metadata.executionTime }}ms</p>
                </div>
                <div>
                    <p><strong>LLM Model:</strong> {{ metadata.model }}</p>
                    <p><strong>Tokens Used:</strong> {{ metadata.tokensUsed | default('N/A') }}</p>
                </div>
            </div>
            <p style="margin-top: 20px; text-align: center;">
                Generated by DProc Framework • PubMed Research Analysis Pipeline v{{ metadata.version }}
            </p>
        </div>
    </div>
</body>
</html>
